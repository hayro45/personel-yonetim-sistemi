<nav class="navbar navbar-default">
    <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/Departman">Personel Yönetim Sistemi</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li class="@(ViewBag.Title == "Departman" ? "active" : "")"><a href="/Departman">Departmanlar</a></li>
                <li class="@(ViewBag.Title == "Personel" ? "active" : "")"><a href="/Personel">Personeller</a></li>
                @if (User.IsInRole("A"))
                {
                    <li class="@(ViewBag.Title == "Kullanicilar" ? "active" : "")"><a href="/Kullanicis">Kullanıcılar</a></li>
                }
                <li><a href="/Security/Logout">Çıkış Yap</a></li>
            </ul>
           
            <p class="navbar-text navbar-right" style="margin-bottom:0px">
                <button class="btn btn-danger"disabled type="button">
                    Toplam Maaş <span class="badge">@{Html.RenderAction("ToplamMaas", "Personel");} ₺</span>
                </button> 
                Hoş Geldiniz 
                <a href="#" class="navbar-link">@HttpContext.Current.User.Identity.Name</a>
            </p>
        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>